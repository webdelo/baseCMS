var editMode = function () {
};

editMode.initForm = function () {

};

editMode.initButtons = function () {

};

editMode.toggleForm = function () {
    if () {
        editMode.
    } else {

    }
};

editMode.showForm = function () {
    $(this).parents().find('.normalMode').hide();
    $(this).parents().find('.editMode').fadeIn();
};

editMode.hideForm = function () {
    $(this).parents().find('.editMode').hide();
    $(this).parents().find('.normalMode').fadeIn();

};

editMode.getForm = function () {

};

editMode.getToggleButton = function () {

};